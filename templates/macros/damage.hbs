<form class="{{cssClass}}" autocomplete="off">
  <section class="sheet-body">
    <p>This script is for creating a custom damage message and applying the damage to your targeted token.</p>
    <div class="form-group">
      <label class="">Label:</label>
      <input name="label" value="{{lookup storedData (lookup storageKeys 0)}}">
    </div>
    <div class="form-group">
      <label>Description:</label>
      <input name="description" value="{{lookup storedData (lookup storageKeys 1)}}">
    </div>
    <div class="form-group">
      <label>Formula:</label>
      <input name="formula" value="{{lookup storedData (lookup storageKeys 2)}}">
    </div>
  </section>
</form>